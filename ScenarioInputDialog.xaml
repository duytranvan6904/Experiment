<Window x:Class="Microsoft.Samples.Kinect.BodyBasics.ScenarioInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Enter Scenario ID" 
        Height="180" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="14" FontWeight="Bold" Margin="0 0 0 10">
            Recording Stopped
        </TextBlock>

        <TextBlock Grid.Row="1" Margin="0 0 0 8">
            Please enter the Scenario ID (1-18):
        </TextBlock>

        <TextBox Name="txtScenarioId" Grid.Row="2" 
                 Width="100" HorizontalAlignment="Left"
                 FontSize="14" Padding="5"
                 Margin="0 0 0 10" />

        <TextBlock Name="txtScenarioInfo" Grid.Row="3" 
                   Foreground="Gray" FontSize="11"
                   TextWrapping="Wrap" Margin="0 0 0 10" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="btnOK" Width="80" Margin="0 0 8 0" Click="BtnOK_Click" IsDefault="True">OK</Button>
            <Button Name="btnCancel" Width="80" Click="BtnCancel_Click" IsCancel="True">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>
